#!/bin/bash
# Placeholder for future boot-time sshd configuration.
# AuthorizedKeysFile is in the base sshd_config (image time).
# Password auth is always enabled; pubkey takes precedence when keys exist.
